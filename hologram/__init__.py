"""Context Grounding — open method package (probe harness + OKF conformance).
Deliberately minimal: the hologram routing runtime (mcp/, system, router,
pressure, cortex-v2, mesh) is NOT part of this public repo. See docs/ for the
open/runtime split rationale."""
